package plan

import "github.com/AbhaySingh002/supremo/internal/prompts"

// PolicyPrompt is the authoritative Plan Mode policy injected into requests when Plan Mode is active.
const PolicyPrompt = prompts.PlanModePolicy
